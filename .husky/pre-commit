pnpm run prettier:write
pnpm run eslint

echo "Running pre-commit hook"

run_command() {
  echo "Running: $1"
  sh -c "$1" || {
    echo "Command failed: $1"
    exit 1
  }
}

run_command "pnpm build"
run_command "pnpm storybook:build"

echo "All pre-commit checks passed!"