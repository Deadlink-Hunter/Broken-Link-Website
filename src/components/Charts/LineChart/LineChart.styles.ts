import { CSSProperties } from 'react';

export const styles = {
  chartContainer: {
    padding: '1.5rem',
    backgroundColor: '#1a1b1e',
    borderRadius: '8px',
    border: '1px solid #373a40',
  } as CSSProperties,

  chartTitle: {
    marginBottom: '1rem',
    color: '#ffffff',
    fontSize: '1.25rem',
    fontWeight: 600,
  } as CSSProperties,
};