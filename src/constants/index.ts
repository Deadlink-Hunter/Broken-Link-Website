import { HERO_CONSTANTS } from './hero.consts';

// Centralized constants export. Future/global constants should be added here.
export const CONSTANTS = {
  HERO: HERO_CONSTANTS,
};

// Backwards compatible default export & named re-export
export { HERO_CONSTANTS };
